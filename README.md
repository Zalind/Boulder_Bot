Boulder Bot

Features
-bouldercheck: takes a picture of boulders cage at the current time, adds a timestamp to the image and sends it back to the user
-celsius x: conversts x into celsius, assuming x is in fahrenheit
-fahrenheit x: converst x into celsius, assuming x is in celsius
-keeps track of chat statistics